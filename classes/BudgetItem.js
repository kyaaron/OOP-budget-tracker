export class BudgetItem {
    constructor(date, title, amount, description) {
        this.date = date;
        this.title = title;
        this.amount = amount;
        this.description = description;
    }
    add(date, title, amount, description) {
        // code to add to amounts
    }
}